# Example backend environment variables
# Replace values with your Atlas connection string or host:port list

# Preferred: standard (non-SRV) connection string when SRV DNS lookups fail
# Obtain from Atlas: Connect -> Drivers -> Select "Standard connection string (includes replica set and port list)"
MONGODB_URI_NON_SRV="mongodb://host1:27017,host2:27017/<dbname>?replicaSet=YourReplicaSet&ssl=true&authSource=admin"

# Or use the SRV connection (requires SRV DNS lookups to work in your environment)
# MONGODB_URI="mongodb+srv://<user>:<pass>@cluster0.example.mongodb.net/<dbname>?retryWrites=true&w=majority"

# Encryption key for AES-256-CBC message encryption (32 hex characters = 16 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: In production, use a secure, randomly generated key and keep it secret
ENCRYPTION_KEY="123f2134ab56cd78ef90123456789abcdeffedcba9876543210fedcba9876543210"

